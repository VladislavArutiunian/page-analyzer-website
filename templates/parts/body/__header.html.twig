<header class="flex-shrink-0">
    <nav class="navbar navbar-expand-md navbar-dark bg-dark px-3">
        <a class="navbar-brand" href="{{ url_for('main') }}">Анализатор страниц</a>
        <button class="navbar-toggler" data-toggle="collapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav">
                <li class="nav-item"><a class="nav-link {{ headerMainActive ?? '' }}" href="{{ url_for('main') }}">Главная</a></li>
                <li class="nav-item"><a class="nav-link {{ headerSitesActive ?? '' }}" href="{{ url_for('urls') }}">Сайты</a></li>
            </ul>
        </div>
    </nav>
</header>